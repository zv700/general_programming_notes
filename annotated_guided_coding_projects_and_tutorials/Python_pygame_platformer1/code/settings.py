import pygame, sys 
# install pygame-ce, unistall pygame before installing pygame-ce -> still imports as pygame
# pygame is outdated
# pygame-ce has more features, runs better, is backwards compatible with progs created with pygame
from pygame.math import Vector2 as vector
# vector2 creates a vector using 2 inputs: x, y

# link settings.py to main.py -> import

#Game window setup
WINDOW_WIDTH, WINDOW_HEIGHT = 1280, 720 #in pixels
TILE_SIZE = 64
ANIMATION_SPEED = 6


